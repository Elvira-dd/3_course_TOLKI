<% content_for :title, "Все подкасты" %>
<% set_meta_tags(
  title: "Admin Panel | Список подкастов",
  keywords: "Подкасты, Рецензии, Информация"
) %>

<h1>Подкасты</h1>

<div id="podcasts" class="allPodcastsPage">
    <% @podcasts.each do |podcast| %>
    <%= render podcast %>
    <% end %>
</div>